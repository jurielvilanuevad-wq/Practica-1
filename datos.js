/*
Este archivo pertenece a la capa de datos.
Unicamente se encarga de recibir, gestionar y proporcionar los datos
a la capa de la lógica de negocio
*/

function obtenerResultado(a,b){

    // Se retorna la suma de los dos valores recibidos como parámetros
    return a+b;
}